pub struct QuantumVM;

impl QuantumVM {
    /// Create a new QuantumVM instance
    pub fn new() -> Self {
        Self
    }
}

impl Default for QuantumVM {
    fn default() -> Self {
        Self::new()
    }
}

pub struct Bytecode;
pub struct Instruction;
